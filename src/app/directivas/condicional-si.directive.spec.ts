import { CondicionalSiDirective } from './condicional-si.directive';

describe('CondicionalSiDirective', () => {
  it('should create an instance', () => {
    const directive = new CondicionalSiDirective();
    expect(directive).toBeTruthy();
  });
});
